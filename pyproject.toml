[project]
name = "reflexes-practice"
version = "0.1.0"
description = "Reaction time test game."
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.uv]
package = false
